name: NativePHP Mobile Build
description: Community GitHub Action for building NativePHP Mobile Android and iOS applications.
author: mrpunyapal
branding:
  icon: smartphone
  color: purple

inputs:
  platform:
    description: Target platform (android or ios).
    required: false
    default: android
  build-type:
    description: Android debug/release/bundle or iOS release.
    required: false
    default: release
  working-directory:
    description: Directory containing the Laravel NativePHP application.
    required: false
    default: .
  php-version:
    description: PHP version passed to setup-php.
    required: false
    default: '8.4'
  node-version:
    description: Node.js version passed to setup-node.
    required: false
    default: '22'
  java-version:
    description: Java version used by the Android Gradle build.
    required: false
    default: '17'
  android-api-level:
    description: Android platform SDK installed for the build.
    required: false
    default: '36'
  android-build-tools:
    description: Android build-tools version installed for the build.
    required: false
    default: '36.0.0'
  android-ndk:
    description: Android NDK version required by the NativePHP Android template.
    required: false
    default: 27.0.12077973
  keystore:
    description: Base64-encoded Android JKS/keystore content. Store it in GitHub Secrets.
    required: false
  keystore-password:
    description: Android keystore password.
    required: false
  key-alias:
    description: Android signing key alias.
    required: false
  key-password:
    description: Android signing key password.
    required: false
  ios-certificate:
    description: Base64-encoded iOS distribution certificate (.p12).
    required: false
  ios-certificate-password:
    description: Password for the iOS certificate.
    required: false
  ios-provisioning-profile:
    description: Base64-encoded iOS provisioning profile.
    required: false
  ios-team-id:
    description: Apple Developer Team ID.
    required: false
  ios-export-method:
    description: iOS export method.
    required: false
    default: app-store
  google-services-json:
    description: Base64-encoded google-services.json content (Firebase Android config). Store it in GitHub Secrets.
    required: false
  google-services-json-path:
    description: Destination path, relative to working-directory, for the decoded google-services.json file.
    required: false
    default: resources/google-services.json
  google-service-info-plist:
    description: Base64-encoded GoogleService-Info.plist content (Firebase iOS config). Store it in GitHub Secrets.
    required: false
  google-service-info-plist-path:
    description: Destination path, relative to working-directory, for the decoded GoogleService-Info.plist file.
    required: false
    default: resources/GoogleService-Info.plist

outputs:
  artifact:
    description: Absolute path to the generated APK, AAB, or IPA.
    value: ${{ steps.artifact.outputs.artifact }}

runs:
  using: composite
  steps:
    - name: Validate NativePHP build inputs
      shell: bash
      env:
        INPUT_PLATFORM: ${{ inputs.platform }}
        INPUT_BUILD_TYPE: ${{ inputs.build-type }}
        INPUT_KEYSTORE: ${{ inputs.keystore }}
        INPUT_KEYSTORE_PASSWORD: ${{ inputs.keystore-password }}
        INPUT_KEY_ALIAS: ${{ inputs.key-alias }}
        INPUT_KEY_PASSWORD: ${{ inputs.key-password }}
        INPUT_IOS_CERTIFICATE: ${{ inputs.ios-certificate }}
        INPUT_IOS_CERTIFICATE_PASSWORD: ${{ inputs.ios-certificate-password }}
        INPUT_IOS_PROVISIONING_PROFILE: ${{ inputs.ios-provisioning-profile }}
        INPUT_IOS_TEAM_ID: ${{ inputs.ios-team-id }}
      run: node "${{ github.action_path }}/scripts/validate.mjs"

    - name: Set up PHP
      uses: shivammathur/setup-php@b604ade2a87db23f8871b7182e69ec5e75effb45 # v2
      with:
        php-version: ${{ inputs.php-version }}
        extensions: mbstring, intl, zip, pcntl, pdo_sqlite
        tools: composer:v2
        coverage: none

    - name: Set up Node.js
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
      with:
        node-version: ${{ inputs.node-version }}

    - name: Set up Java
      if: ${{ inputs.platform == 'android' }}
      uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6
      with:
        distribution: temurin
        java-version: ${{ inputs.java-version }}

    - name: Set up Android SDK
      if: ${{ inputs.platform == 'android' }}
      uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4
      with:
        packages: platform-tools

    - name: Install Android build dependencies
      if: ${{ inputs.platform == 'android' }}
      shell: bash
      env:
        ANDROID_API_LEVEL: ${{ inputs.android-api-level }}
        ANDROID_BUILD_TOOLS: ${{ inputs.android-build-tools }}
        ANDROID_NDK: ${{ inputs.android-ndk }}
      run: |
        set -euo pipefail
        echo "NativePHP Build"
        echo "Platform: android"
        echo "Build type: ${{ inputs.build-type }}"
        echo "Installing Android platform $ANDROID_API_LEVEL, build-tools $ANDROID_BUILD_TOOLS, NDK $ANDROID_NDK..."
        yes | sdkmanager --licenses >/dev/null || true
        sdkmanager \
          "platforms;android-${ANDROID_API_LEVEL}" \
          "build-tools;${ANDROID_BUILD_TOOLS}" \
          "cmake;3.22.1" \
          "ndk;${ANDROID_NDK}"

    - name: Cache Composer and npm downloads
      uses: actions/cache@6849a6489940f00c2f30c0fb92c6274307ccb58a # v4
      with:
        path: |
          ~/.composer/cache
          ~/.npm
        key: ${{ runner.os }}-nativephp-dependencies-${{ hashFiles('**/composer.lock', '**/package-lock.json', '**/package.json') }}
        restore-keys: |
          ${{ runner.os }}-nativephp-dependencies-

    - name: Cache Gradle downloads
      if: ${{ inputs.platform == 'android' }}
      uses: actions/cache@6849a6489940f00c2f30c0fb92c6274307ccb58a # v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-nativephp-gradle-${{ inputs.android-api-level }}-${{ inputs.android-build-tools }}-${{ inputs.android-ndk }}
        restore-keys: |
          ${{ runner.os }}-nativephp-gradle-

    - name: Install application dependencies
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        set -euo pipefail
        echo "Installing Composer dependencies..."
        composer install --no-interaction --prefer-dist --no-progress
        if [ -f package-lock.json ]; then
          echo "Installing npm dependencies..."
          npm ci
        elif [ -f package.json ]; then
          echo "Installing npm dependencies from package.json..."
          npm install --no-audit --no-fund
        fi
        if node -e "const p=require('./package.json'); process.exit(p.scripts?.build ? 0 : 1)" 2>/dev/null; then
          echo "Building frontend assets..."
          npm run build
        fi

    - name: Decode Firebase configuration files
      if: ${{ inputs.google-services-json != '' || inputs.google-service-info-plist != '' }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        GOOGLE_SERVICES_JSON_BASE64: ${{ inputs.google-services-json }}
        GOOGLE_SERVICES_JSON_PATH: ${{ inputs.google-services-json-path }}
        GOOGLE_SERVICE_INFO_PLIST_BASE64: ${{ inputs.google-service-info-plist }}
        GOOGLE_SERVICE_INFO_PLIST_PATH: ${{ inputs.google-service-info-plist-path }}
      run: |
        set -euo pipefail
        decode_base64() {
          if base64 --help 2>&1 | grep -q -- '--decode'; then
            base64 --decode
          else
            base64 -D
          fi
        }
        resolve_safe_path() {
          BASE_DIR="$(pwd)" TARGET_PATH="$1" node "${{ github.action_path }}/scripts/resolve-safe-path.mjs"
        }
        if [ -n "$GOOGLE_SERVICES_JSON_BASE64" ]; then
          resolved_path="$(resolve_safe_path "$GOOGLE_SERVICES_JSON_PATH")"
          mkdir -p "$(dirname "$resolved_path")"
          printf '%s' "$GOOGLE_SERVICES_JSON_BASE64" | decode_base64 > "$resolved_path"
          test -s "$resolved_path"
          echo "Decoded google-services.json to $resolved_path"
        fi
        if [ -n "$GOOGLE_SERVICE_INFO_PLIST_BASE64" ]; then
          resolved_path="$(resolve_safe_path "$GOOGLE_SERVICE_INFO_PLIST_PATH")"
          mkdir -p "$(dirname "$resolved_path")"
          printf '%s' "$GOOGLE_SERVICE_INFO_PLIST_BASE64" | decode_base64 > "$resolved_path"
          test -s "$resolved_path"
          echo "Decoded GoogleService-Info.plist to $resolved_path"
        fi

    - name: Prepare NativePHP project
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        set -euo pipefail
        echo "Preparing NativePHP..."
        php artisan native:install "${{ inputs.platform }}" --force --no-interaction

    - name: Configure mobile runtime environment
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        set -euo pipefail
        if [ ! -f .env ] && [ -f .env.example ]; then
          cp .env.example .env
        fi
        if ! grep -q '^NATIVEPHP_RUNNING=' .env; then
          printf '\nNATIVEPHP_RUNNING=true\n' >> .env
        else
          awk '{ if ($0 ~ /^NATIVEPHP_RUNNING=/) print "NATIVEPHP_RUNNING=true"; else print }' .env > .env.nativephp.tmp
          mv .env.nativephp.tmp .env
        fi
        if ! grep -q '^APP_KEY=base64:' .env; then
          php artisan key:generate --force --no-interaction
        fi

    - name: Decode Android signing keystore
      if: ${{ inputs.platform == 'android' && inputs.build-type != 'debug' }}
      shell: bash
      env:
        KEYSTORE_BASE64: ${{ inputs.keystore }}
      run: |
        set -euo pipefail
        key_path="$RUNNER_TEMP/nativephp-build-action.keystore"
        printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$key_path"
        test -s "$key_path"
        echo "NATIVEPHP_BUILD_KEYSTORE_PATH=$key_path" >> "$GITHUB_ENV"

    - name: Generate temporary development keystore
      if: ${{ inputs.platform == 'android' && inputs.build-type == 'debug' }}
      shell: bash
      run: |
        set -euo pipefail
        key_path="$RUNNER_TEMP/nativephp-build-action.keystore"
        keytool -genkeypair -v \
          -keystore "$key_path" \
          -storepass changeit \
          -keypass changeit \
          -alias nativephp-development \
          -keyalg RSA \
          -keysize 2048 \
          -validity 1 \
          -dname "CN=NativePHP Development"
        echo "NATIVEPHP_BUILD_KEYSTORE_PATH=$key_path" >> "$GITHUB_ENV"
        echo "NATIVEPHP_BUILD_KEYSTORE_PASSWORD=changeit" >> "$GITHUB_ENV"
        echo "NATIVEPHP_BUILD_KEY_ALIAS=nativephp-development" >> "$GITHUB_ENV"
        echo "NATIVEPHP_BUILD_KEY_PASSWORD=changeit" >> "$GITHUB_ENV"

    - name: Decode iOS signing files
      if: ${{ inputs.platform == 'ios' }}
      shell: bash
      env:
        IOS_CERTIFICATE_BASE64: ${{ inputs.ios-certificate }}
        IOS_PROVISIONING_PROFILE_BASE64: ${{ inputs.ios-provisioning-profile }}
      run: |
        set -euo pipefail
        certificate_path="$RUNNER_TEMP/nativephp-ios-certificate.p12"
        profile_path="$RUNNER_TEMP/nativephp-ios-profile.mobileprovision"
        decode_base64() {
          if base64 --help 2>&1 | grep -q -- '--decode'; then
            base64 --decode
          else
            base64 -D
          fi
        }
        printf '%s' "$IOS_CERTIFICATE_BASE64" | decode_base64 > "$certificate_path"
        printf '%s' "$IOS_PROVISIONING_PROFILE_BASE64" | decode_base64 > "$profile_path"
        test -s "$certificate_path"
        test -s "$profile_path"
        echo "NATIVEPHP_IOS_CERTIFICATE_PATH=$certificate_path" >> "$GITHUB_ENV"
        echo "NATIVEPHP_IOS_PROFILE_PATH=$profile_path" >> "$GITHUB_ENV"

    - name: Build NativePHP Android application
      if: ${{ inputs.platform == 'android' }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        INPUT_BUILD_TYPE: ${{ inputs.build-type }}
        INPUT_KEYSTORE_PASSWORD: ${{ inputs.keystore-password }}
        INPUT_KEY_ALIAS: ${{ inputs.key-alias }}
        INPUT_KEY_PASSWORD: ${{ inputs.key-password }}
      run: |
        set -euo pipefail
        mkdir -p "$RUNNER_TEMP/nativephp-artifacts"
        export ANDROID_KEYSTORE_FILE="$NATIVEPHP_BUILD_KEYSTORE_PATH"
        if [ "$INPUT_BUILD_TYPE" = "debug" ]; then
          export ANDROID_KEYSTORE_PASSWORD="$NATIVEPHP_BUILD_KEYSTORE_PASSWORD"
          export ANDROID_KEY_ALIAS="$NATIVEPHP_BUILD_KEY_ALIAS"
          export ANDROID_KEY_PASSWORD="$NATIVEPHP_BUILD_KEY_PASSWORD"
          package_build_type=release
        else
          export ANDROID_KEYSTORE_PASSWORD="$INPUT_KEYSTORE_PASSWORD"
          export ANDROID_KEY_ALIAS="$INPUT_KEY_ALIAS"
          export ANDROID_KEY_PASSWORD="$INPUT_KEY_PASSWORD"
          package_build_type="$INPUT_BUILD_TYPE"
        fi
        echo "Building Android application..."
        php artisan native:package android \
          --build-type="$package_build_type" \
          --output="$RUNNER_TEMP/nativephp-artifacts" \
          --no-tty \
          --no-interaction

    - name: Build NativePHP iOS application
      if: ${{ inputs.platform == 'ios' }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        IOS_CERTIFICATE_PASSWORD: ${{ inputs.ios-certificate-password }}
        IOS_TEAM_ID: ${{ inputs.ios-team-id }}
        IOS_EXPORT_METHOD: ${{ inputs.ios-export-method }}
      run: |
        set -euo pipefail
        mkdir -p "$RUNNER_TEMP/nativephp-artifacts"
        echo "Building iOS application..."
        php artisan native:package ios \
          --build-type=release \
          --output="$RUNNER_TEMP/nativephp-artifacts" \
          --certificate-path="$NATIVEPHP_IOS_CERTIFICATE_PATH" \
          --certificate-password="$IOS_CERTIFICATE_PASSWORD" \
          --provisioning-profile-path="$NATIVEPHP_IOS_PROFILE_PATH" \
          --team-id="$IOS_TEAM_ID" \
          --export-method="$IOS_EXPORT_METHOD" \
          --no-tty \
          --no-interaction

    - name: Print NativePHP build diagnostics
      if: ${{ failure() }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        set +e
        for log in nativephp/android-build.log nativephp/ios-build.log; do
          if [ -f "$log" ]; then
            echo "--- $log ---"
            cat "$log"
          fi
        done
        find nativephp -maxdepth 4 -type f \( -name '*.log' -o -name 'composer-*.json' \) -print 2>/dev/null

    - name: Locate build artifact
      id: artifact
      shell: bash
      env:
        BUILD_TYPE: ${{ inputs.build-type }}
        PLATFORM: ${{ inputs.platform }}
        ARTIFACT_DIRECTORY: ${{ runner.temp }}/nativephp-artifacts
      run: node "${{ github.action_path }}/scripts/find-artifact.mjs"

    - name: Remove temporary signing material
      if: ${{ always() }}
      shell: bash
      run: rm -f "$RUNNER_TEMP/nativephp-build-action.keystore" "$RUNNER_TEMP/nativephp-ios-certificate.p12" "$RUNNER_TEMP/nativephp-ios-profile.mobileprovision"

    - name: Remove decoded Firebase configuration files
      if: ${{ always() && (inputs.google-services-json != '' || inputs.google-service-info-plist != '') }}
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        GOOGLE_SERVICES_JSON_PATH: ${{ inputs.google-services-json-path }}
        GOOGLE_SERVICE_INFO_PLIST_PATH: ${{ inputs.google-service-info-plist-path }}
      run: |
        set +e
        resolve_safe_path() {
          BASE_DIR="$(pwd)" TARGET_PATH="$1" node "${{ github.action_path }}/scripts/resolve-safe-path.mjs" 2>/dev/null
        }
        resolved_path="$(resolve_safe_path "$GOOGLE_SERVICES_JSON_PATH")" && rm -f "$resolved_path"
        resolved_path="$(resolve_safe_path "$GOOGLE_SERVICE_INFO_PLIST_PATH")" && rm -f "$resolved_path"
        exit 0
